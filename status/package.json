{
  "name": "relaxy-status",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Publishes status.json for relaxy.xyz: the Pi's own vitals, plus where Relaxy! is currently running (read from the `hosts` registry in MongoDB). Drop-in replacement for additions/status-writer.sh.",
  "main": "main.mjs",
  "scripts": {
    "start": "node main.mjs",
    "once": "node main.mjs --once"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
